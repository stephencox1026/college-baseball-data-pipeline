# Copy to .env and fill in. NEVER commit the real .env.

# Database (used by Python ETL and Node API)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=college_baseball
DB_USER=postgres
DB_PASSWORD=postgres

# Node API (optional)
# PORT=8080
# LOG_LEVEL=info
# DB_POOL_MAX=10
